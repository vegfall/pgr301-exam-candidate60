#As task asked for an email in code (and not in github variables) I have added a mock email so not to expose my own email. Will work when changed to a real email.
notification_email = "mock@email.com"
sqs_queue_name     = "image-request-queue-60"
